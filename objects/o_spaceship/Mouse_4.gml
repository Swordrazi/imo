/*
/// @description Clicking to Multiply
clicks_++;
if (clicks_ >= clicks_to_multiply_) {
	clicks_ -= clicks_to_multiply_;
	clicks_to_multiply_ *= ctm_multiplier_;
	multiplier_ += multiplier_add_;
}
image_index = 1;
image_speed = 15;
